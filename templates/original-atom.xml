<?xml version="1.0" encoding="UTF-8"?>
{%- set posts = get_section(path="feed/blog/_index.md") -%}
{%- set reviews = get_section(path="feed/reviews/_index.md") -%}
{%- set links = get_section(path="feed/links/_index.md") -%}
{%- set talks = get_section(path="feed/talks/_index.md") -%}
{%- set til = get_section(path="feed/til/_index.md") -%}
{%- set items = posts.pages | concat(with=reviews.pages) | concat(with=links.pages) | concat(with=talks.pages) | concat(with=til.pages) | sort(attribute="date") | reverse | slice(end=50) %}
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
  <title>{{ config.title }} - original posts</title>
  <subtitle>Posts, reviews, links, talks, and TILs by {{ config.extra.author }} (no imported social activity)</subtitle>
  <link rel="self" type="application/atom+xml" href="{{ get_url(path="original.atom.xml") | safe }}"/>
  <link rel="alternate" type="text/html" href="{{ config.base_url | safe }}/feed/"/>
  <id>{{ get_url(path="original.atom.xml") | safe }}</id>
  <updated>{% if items %}{{ items.0.date | date(format="%+") }}{% else %}{{ now() | date(format="%+") }}{% endif %}</updated>
  <author><name>{{ config.extra.author }}</name></author>
  {%- for page in items %}
  <entry xml:lang="en">
    <title>{{ page.title }}</title>
    <published>{{ page.date | date(format="%+") }}</published>
    <updated>{{ page.updated | default(value=page.date) | date(format="%+") }}</updated>
    <author><name>{{ config.extra.author }}</name></author>
    <link rel="alternate" type="text/html" href="{{ page.permalink | safe }}"/>
    <id>{{ page.permalink | safe }}</id>
    <content type="html">{{ page.content }}</content>
  </entry>
  {%- endfor %}
</feed>
